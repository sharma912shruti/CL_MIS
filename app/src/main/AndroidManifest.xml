<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mis.cl.com.cl_mis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.AutoApprovalActivity" />

        <activity android:name=".activity.MechinePendingApprovalActivity" />

        <activity android:name=".activity.NewCenterActivity" />

        <activity android:name=".activity.PendingApprovalActivity" />

        <activity android:name=".activity.ValidationActivity" />

        <activity android:name=".activity.LoginActivity" />

        <activity android:name=".activity.BaseActivity" />

    </application>

</manifest>